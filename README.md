This is a project to deploy on ESP8266 that can pulse a relay to start a computer after a power outage. (Only needed for simple bios setup that do not have AutoOn after power outage)

```sh
cp secrets.h.template secrets.h

# And then complete the variables before compile
```